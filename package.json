{
  "name": "ce2-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test":  "pnpm -r test",
    "lint":  "pnpm -r lint",
    "dev":   "pnpm -r dev"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "tsup": "^8.0.2"
  }
}
